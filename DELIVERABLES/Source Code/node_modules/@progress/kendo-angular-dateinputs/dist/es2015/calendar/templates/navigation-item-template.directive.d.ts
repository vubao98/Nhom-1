import { TemplateRef } from '@angular/core';
/**
 * Used for rendering the navigation item of the Calendar.
 *
 * To define the navigation item template, nest an `<ng-template>` tag with the `kendoCalendarNavigationItemTemplate` directive
 * inside the component tag.
 *
 * The template context is set to the current component. To get a reference to the current item value, use the `let-title` directive.
 * To provide more details about the current title, get a reference to the current `date`
 * by using the `let-date='date'` directive or get a reference to the current active view
 * by using the `let-activeView='activeView'` directive.
 *
 * @example
 * ```ts
 * _@Component({
 * selector: 'my-app',
 * styles: ['.custom { color: red; }'],
 * template: `
 *  <kendo-calendar>
 *    <ng-template kendoCalendarNavigationItemTemplate let-title>
 *      <span class="custom">{{title}}</span>
 *    </ng-template>
 *  </kendo-calendar>
 * `
 * })
 * class AppComponent { }
 * ```
 *
 * For more examples, refer to the section on [templates]({% slug overview_calendar %}#toc-templates).
 */
export declare class NavigationItemTemplateDirective {
    templateRef: TemplateRef<any>;
    constructor(templateRef: TemplateRef<any>);
}
